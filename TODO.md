# TODO

## 高优先级
- [ ] **盘中成交过的当天，净值曲线上是个缺口**（2026-09-18 真库实测发现）
      `PaperTradingService.evaluateStrategy` 开头有一条 `existsByStrategyIdAndTradeDate(...)` 守卫：
      当天已有成交就直接 `return null`。它防的是重复下单（对的），但连带把当天该做的四件事一起跳过了 ——
      **净值快照、客观事实、到期预期回填、当天报告**。
      证据：策略 6 在 15:29:52 由盘中路径成交（trace #1），15:30 的日线结算因此整段跳过，
      当天既没有 daily 痕迹也没有净值点。而"当天有成交"恰好是唯一值得看的那天，
      且净值曲线正是判断策略有没有变好的主依据。
      修的时候要一起想清楚三点：① 不再问任何决策源（agent 一次约 2 万 token，为一次估值不值）；
      ② 当日收盘价从哪来（快照的 close_price 参与买入持有对照）；③ 痕迹要给出**真实原因**
      （不能写成 rule_not_met），大概率要给封闭集加一个 skip 原因并同步两侧镜像测试。
- [ ] 持仓成本分析：用户绑定买入价，策略建议基于实际盈亏
- [ ] 意图缓存：常见问法跳过LLM，提速

## 中优先级
- [ ] 异步回复：先回分析中再追结果
- [ ] 训练日志：记录每次R²，追踪模型退化

## 已完成
- [x] 模型持久化（磁盘pkl）
- [x] 按需/脚本重训（无后台定时调度，与 README 声明保持一致）
- [x] 时序切分（防数据泄漏）
- [x] 三窗口差异化调参
- [x] Prompt审慎化
- [x] 股票名→代码自动转换
- [x] 回复截断