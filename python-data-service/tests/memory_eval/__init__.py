"""记忆质量评测（golden set）。

这里放的是**可回归**的记忆质量检查，而不是又一份单元测试：

- `cases.yaml` 是 golden set 本体（场景 + 期望），非程序员也能加用例；
- `harness.py` 用真实的生产代码路径跑每个用例（`run_agent` → `recall` → 融合 → 渲染），
  只把三处外部依赖换成受控替身：Java 返回的记忆上下文、语义召回名次、LLM 回复；
- `runner.py` 汇总指标（召回命中率、错误注入率、注入体积、安全声明覆盖率），
  输出表格与 JSON 报告，并按阈值判定 PASS/FAIL。

为什么要它：记忆系统的改动（prompt、权重、阈值、预算）大多**不会报错**，
只会让答案悄悄变差。没有固定场景 + 指标，就只能靠感觉判断"这次改动是不是变好了"。
"""
